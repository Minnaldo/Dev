#include <vector>
#include <algorithm>
#include <string>
#include <iostream>

using namespace std;

/**
 * * 도둑질 문제()
 * * 다수의 경우의 수에서 최적해를 찾는 문제 -> 전통적 최적화 문제
 * ? 전통적 최적화문제?
 */

int solution(vector<int> money) {
    int answer = 0;
    return answer;
}

int main(int argc, char const *argv[])
{

    return 0;
}