for (int i = 0; i < finishTime.size(); i++)
    // {
    //     int tmpVal = finishTime.at(i);
    //     int cnt = count(finishTime.begin(), finishTime.end(), tmpVal);
    //     answer.push_back(cnt);        
    //     finishTime.erase(finishTime.begin(), finishTime.begin()+cnt);
    // }
    // int idx = 0;