#include <string>
#include <vector>
#include <iostream>

using namespace std;

string conn(vector<string> &timetable)
{
    string time = "";
    int ttableSize = timetable.size();

    

    return time;
}

int main(int argc, char const *argv[])
{
    vector<string> timetable;
    int n, t, m = 0;
    n, t = 1;
    m = 5;
    timetable ={"08:00", "08:01", "08:02", "08:03"};
    string answer ="";
    answer = conn(timetable);
    cout << "Answer : " << answer << endl;
    return 0;
}
